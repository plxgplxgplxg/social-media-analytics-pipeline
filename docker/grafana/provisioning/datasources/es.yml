apiVersion: 1

datasources:
  - name: Elasticsearch
    type: elasticsearch
    access: proxy
    url: http://elasticsearch:9200
    database: "processed_posts"
    jsonData:
      timeField: "published_at"
      esVersion: "8.0.0"
      tlsSkipVerify: true
    isDefault: true
